{
  "$schema": "../../FZPF_v0.1.schema.json",
  "_comment": "INVALID: Uses regex pattern instead of glob (regex forbidden)",
  "policy": {
    "format": "fzpf",
    "schema_version": "0.1",
    "default_deny": true
  },
  "zones": [
    {
      "id": "z:work",
      "integrity_level": 60,
      "confidentiality_level": 70
    }
  ],
  "zone_policies": [
    {
      "zone_id": "z:work",
      "principal_allow": ["^agent:.*$"]
    }
  ]
}
